const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.service,.pkg,.member,.review,.gallery figure,.intro,.about,.booking>*').forEach(el=>obs.observe(el));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();f.querySelector('.success').classList.add('show')}));